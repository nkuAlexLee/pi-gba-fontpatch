# Elite Redux 中文字库补丁 — 状态（已完成，正向分析路线）

## ✅ 最终交付（v5）
2.65.1 beta reupload 主菜单 "New Game" → **"新游戏"**，字形完整（含右下阴影、右侧无裁剪）：
- 产物：`tmp/EliteRedux_chs_v6.gba` = `testfiles/...新游戏字库.gba`
- 可复现脚本：`fontpatch/patch-elite-redux-2651.js`（支持任意 3 字以内替换，内置自检回路）
- 关键参数：空槽 0x0A/0x18/0x1F，字形数组 @0x1067D18（64B/字 四-tile 2bpp），
  宽度表 @0x106FD18（=12，即两个英文字符位），H_OFF=1 / V_OFF=2 / 右下阴影，
  菜单字符串 @0xEFB11C
- 详细正向分析实录（含全部踩坑）：`.pi/skills/gba-font-crack/EliteRedux-正向分析实录.md`
- 格式/地址速查：`.pi/skills/gba-font-crack/SKILL.md` Elite Redux 节

## 历史路线（已废弃，结论保留供参考）
早期曾尝试移植第三方 PokemonER 补丁（hook 引擎 + work buf 0x030064EC）并修了多个 bug
（r6 破坏、返回点、tileconv 栈泄漏等），但正向分析证明该补丁**原理性不可行**：
1. 失败路径返回点 RenderText+0x9A 落在控制码跳转表内部 → 普通字符必崩
2. +0x23E 是 fontid 二级分发非消费点；0x030064EC 全 ROM 零合法引用
   （真正的工作缓冲 = 0x03005600）
3. `0x08256CB0` 实为 DecompressGlyphTile（引擎内使用），并非"无关函数"

⚠ 不要再尝试移植该补丁；也不要参考旧文档中 "字体数组在 0x08104F528 附近" 的说法
（该地址是字宽表碎片，非字形数组；真实字形数组 = 0x09067D18）。

## 备用路线（未启用）：绿宝石字库.bin 整库插入
- 972672B = 0xED780，记录格式与引擎字形格式**完全相同**（64B 四-tile 2bpp）
- 阻塞点：记录顺序非线性公式（阿=0 符合公式，但 新 在 3222 ≠ 公式 3041），
  疑似按码表条目顺序，需锚字扫描法建立映射后再插入
- 插入方式参考 `gui_related/font_patch.py`：扩容区 ≥0xED788 的 FF 空区，
  基址整八字节对齐，基址/基址+0x80000 双指针，宽度表改 0x0C
- 插入后仍需自写 hook（失败路径回 +0x90 重建 r3、成功路径调 0x082562A8 ×4
  写 0x03005600 + 跳 0x0825728C），详见实录第 6 节
